import { useForm } from "react-hook-form";
import toast from "react-hot-toast";
import { FiMail, FiMapPin, FiSend } from "react-icons/fi";
import Button from "../../../components/ui/Button";

function Contact() { const { register, handleSubmit, reset, formState: { isSubmitting } } = useForm(); async function submit(values) { const subject = encodeURIComponent(`LibConnect enquiry from ${values.name}`); const body = encodeURIComponent(`${values.message}\n\nReply to: ${values.email}`); window.open(`mailto:hello@libconnect.example?subject=${subject}&body=${body}`, "_self"); toast.success("Your email app is opening"); reset(); } return <section className="mx-auto max-w-6xl px-6 py-16"><div className="grid gap-12 lg:grid-cols-[0.9fr_1.1fr]"><div><p className="text-sm font-bold uppercase tracking-widest text-blue-600">Get in touch</p><h1 className="mt-3 text-4xl font-bold">Let’s make your library more connected</h1><p className="mt-5 leading-7 text-slate-600">Questions about LibConnect, library registration, or platform support? Send us a message and we’ll point you in the right direction.</p><div className="mt-8 space-y-4 text-sm text-slate-600"><p className="flex items-center gap-3"><FiMail className="text-blue-600" />hello@libconnect.example</p><p className="flex items-center gap-3"><FiMapPin className="text-blue-600" />Available for libraries everywhere</p></div></div><form onSubmit={handleSubmit(submit)} className="rounded-2xl bg-white p-7 shadow-sm ring-1 ring-slate-100"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium">Name<input required className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5" {...register("name")} /></label><label className="text-sm font-medium">Email<input required type="email" className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5" {...register("email")} /></label><label className="text-sm font-medium sm:col-span-2">Message<textarea required rows="5" className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-2.5" {...register("message")} /></label></div><Button type="submit" disabled={isSubmitting} className="mt-6"><FiSend className="mr-2" />Send message</Button></form></div></section>; }
export default Contact;
